<?php

/* PAGE 1: Build a web Service (API) that json_encodes a SELECT from the database;
 * PAGE 2: Load the JSON object in console.
 * PAGE 2: Loop through the JSON object, only showing the ID, and another varchar value 
 * EXTRA: odd lines get one color, even lines get another
 *
 */

?>
